assembly-versioning-scheme: MajorMinorPatchTag
continuous-delivery-fallback-tag: ''
tag-prefix: ''
branches:
  main:
    regex: main
    increment: Patch
    is-main-branch: true
  feature:
    regex: features?[/-]
    increment: None
